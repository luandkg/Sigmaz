
    require "operadores.sigmaz";
    require "terminal.sigmaz";
    require "cast.sigmaz";

    require "pos.sigmaz";
    require "neg.sigmaz";

    require "organismos.sigmaz";

    stages Numeros -> { POSITIVO, NEGATIVO };
    stages Comparacao -> { MAIOR, MENOR, IGUAL };
    stages Logico -> { TRUE, FALSE, UNKOWN };


