

    mockiz SIGMAZ : string = "SIGMAZ 1.0";
    mockiz VERSAO : num = 1.0;

    define ALGO : string = "__ALGO__";

    require "operadores.sigmaz";
    require "terminal.sigmaz";
    require "cast.sigmaz";

    require "pos.sigmaz";
    require "neg.sigmaz";

    require "organismos.sigmaz";

    stages_def Numeros -> { POSITIVO, NEGATIVO };
    stages_def Comparacao -> { MAIOR, MENOR, IGUAL };
    stages_def Logico -> { TRUE, FALSE, UNKOWN };


