call iniciar -> inicio ( );
define A : num = 10;
act inicio ( ) {
	invoke __COMPILER__ -> SHOW_SCOPE ( ) :: ALL;
}

#struct Relator at Relax {}
struct Contador at M_Contagem {
	define Base : num = 10;
	mockiz Fluxo : num = 12;
	define val : vax >> ( num ) = 12;
	act contar ( a : num ) {
		
	}
	func retornar ( b : num ) : num {
		
	}
	
}
model M_Contagem {

	define Base : num;
	mockiz Fluxo : num;
	define val : vax >> ( num );

	act contar ( num );
	func retornar ( num ) : num;
}
