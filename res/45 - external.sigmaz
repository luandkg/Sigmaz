call iniciar -> inicio ( );
define A : int = 10;
act inicio ( ) {
	def valor : int = 1;
	invoke __COMPILER__ -> SHOW_SCOPE ( ) :: ALL;
	def T : int = COISAS -> CG ( 5 );
	invoke __COMPILER__ -> SHOW_SCOPE ( ) :: ALL;
}
stages DIAS -> {
	SEGUNDA , TERCA 
}
stages JOGOS -> not {
	PRIMEIRA , SEGUNDA 
}
external COISAS {
	define CAZ : int = 10;
	mockiz TREZ : int = 3;
	func CG ( a : int ) : int {
		return a;
	}
	
}
