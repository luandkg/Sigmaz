
require lib;

call iniciar -> inicio ( );

act inicio ( ) {

	def valor : int = 1;
	
	def ternal : int = -> ( maior ( valor , 2 ) ) :: ( +1 ) not ( -1 );
	
	def ale : int =  aleatorio ( 0 , 100 ) ;

	invoke __COMPILER__ -> SHOW_SCOPE ( ) :: STACK;
}
