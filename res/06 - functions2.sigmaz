require "libs/operadores.sigmaz";

call iniciar -> operando ( );

#Luan
act operando ( ) {
	def a1 : num = 10;
	def a2 : num = 10;
	def a3 : bool = true;
	def t1 : bool = a1 == a2;
	def a : num = dividir ( 6 , 2 );
	def b : num = 15;
	def c : num = 2;

	# Bora
	def bb : num = somar ( 2 , 5 );
	def bl : bool = true;
	def ll : bool = igual ( bl , igual ( false , false ) );
	invoke __COMPILER__ -> SHOW_SCOPE ( ) :: ALL;
}
