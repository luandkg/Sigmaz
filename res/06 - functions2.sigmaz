
require "operadores.sigmaz";

call iniciar -> operando();


act operando(){


    def a1 : num = 10;
    def a2 : num = 10;
    def a3: bool = true;

    def t1 : bool = a1 == a2;

    def a : num = dividir(6,2);

    def b : num = 15;
    def c : num = 2;

    def e : bool = b == somar(10,5);


    def ll : bool = igual(e,igual(false,true));

    invoke __COMPILER__ -> SHOW_SCOPE () :: ALL;

}

func igual(a:bool,b:bool) : bool {
    return b;
}