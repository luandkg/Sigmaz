struct Item in ( T ) {
	restrict : 
	define mGuardar : T = null;
	define mNivel : T;
	all : 
	act guardar ( eGuardar : T ) {
		mGuardar = eGuardar;
	}

	act nivelar ( eNivel : T ) {
		mNivel = eNivel;
	}
	func passar ( ) : T {
		return mGuardar;
	}

	act cortar ( a : num ) {
		def b : num;
		invoke terminal -> print ( a ) :: b;
	}
	
}

struct Padrao {
	
}
call iniciar -> inicio ( );
act inicio ( ) {
	def P1 : Padrao = init Padrao ( );
	def P2 : Padrao >> ( ) = init Padrao ( );
	def P3 : Padrao >> ( ) = init Padrao ( ) >> ( );
	def P4 : Padrao = init Padrao ( ) >> ( );
	def N1 : Item >> ( num ) = init Item ( ) >> ( num );
	def N2 : Item >> ( num ) = init Item ( ) >> ( num );
	def G : Item >> ( Item >> ( num ) ) = init Item ( ) >> ( Item >> ( num ) );
	def sa : Item >> ( num ) = init Item ( ) >> ( num );
	sa.nivelar ( 2 );
	G.guardar ( sa );
	G.passar ( ).cortar ( 5 );
	invoke __COMPILER__ -> SHOW_SCOPE ( ) :: STACK;
}
